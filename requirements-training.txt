# Optional: required only for LoRA training and the transformers_peft inference backend.
# A CUDA-enabled PyTorch build is strongly recommended for medium/large models.
torch>=2.3
transformers>=4.46
peft>=0.13
accelerate>=1.0
safetensors>=0.4
